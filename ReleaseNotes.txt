Version 2.0.0          5th December 2025
========================================
This is the first stable release of VecGeom 2.0.0.
It consolidates all changes from the 2.0.0-rc1…rc9 pre-releases, bringing:
- A new GPU-friendly “frames” surface model.
- A complete rewrite and stabilization of GenericTrapezoid.
- Major robustness improvements in navigation and BVH.
- Overhauled CUDA RDC/build tooling and portability fixes.
- New logging/diagnostics and device APIs.
- Significant API cleanup and removal of legacy vector interfaces.

Below is a high-level summary of the main areas.

Overview
- Introduced a surface-model framework (“frames”) for accelerated navigation
using bounded half-spaces and BVHs on GPU.
- Fully refactored GenericTrapezoid (GenTrap) with robust algorithms, better 
numerical behavior, and improved CPU/GPU performance.
- Strengthened global navigation (BVH/Looper navigators, NavState helpers, 
NavigationTable) for large, complex geometries.
- Modernized CUDA RDC integration and build-system handling, improving 
compatibility with recent compilers and toolchains.
- Introduced unified logging facilities, new device APIs, and portability 
macros.
- Removed legacy vector APIs and obsolete infrastructure, simplifying the 
public interface.

Surface Model (“frames”)
- Added the frames surface model for Geant-like hierarchical 3D solid 
modeling based on bounded half-spaces.
- Provides a header-only, stateless GPU navigation API, mirroring VecGeom 
solid navigation interfaces.
- Supports planar, cylindrical, conical half-spaces and frame types (window, 
ring, etc.) to define bounded regions.
- Introduced FramedSurface objects storing solid–surface intersections for 
efficient navigation and ray tracing.
- Designed to be GPU-friendly: avoids recursion and virtuals, reduces 
register/stack pressure, and mitigates divergence.
- Supports double and mixed-precision modes and uses BVHs for fast collision 
detection and ray tracing.
- Replaced the GPU solids hierarchy with an indexed LogicId/PlacedId scheme 
for improved performance and memory usage.

Solids / Geometry
- GenericTrapezoid (GenTrap)
  - Major scalar refactor with documented algorithms and improved numerical
  robustness.
  - Implemented Lipschitz-based conservative safety, improved Inside() / Normal() 
  handling, and support for difficult aspect ratios.
  - Added caching, CUDA-side optimizations, and extensive edge-case tests 
  (e.g. EMEC bad aspect-ratio configuration).
  - Numerous micro-optimizations and navigation fixes tied to GenTrap.

- Polycone / Polyhedron / Tube / Sphere / Torus
  - Polycone:
    - New SafetyToOut() based on the line segment forming the boundary, fixing
    all known safety issues.
    - Fixes for generic polycone construction logic and RZ polycones; improved 
    ReducedPolycone segment checks.

  - Polyhedron:
    - Introduced a stable Z-plane index finder and fixes in PolyhedronStruct.
  - Tube:
    - Corrected Y-component typo in phi-plane normal calculation.
  - Sphere:
    - Fixed DistanceToIn() for robust ray entry behavior.
  - Torus:
    - Fixes for phi cuts in corner cases and rescaling in radius calculations.

- Other shapes and geometry utilities
  - Contains() improved across shapes to correctly handle points on surfaces 
  and reduce boundary artifacts.
  - Polygon and Box: added small tolerances in edge/face tests to prevent crashes 
  during relocation.
  - Scaled shapes and trapezoids: fixed normal computations.
  - Torus cleanup: removed duplicated data between UnplacedTorus2 and torus struct; 
  fixed bounding tube initialization.
  - VUnplacedVolume::Extent exposed as a pure virtual interface; unplaced volumes 
  must provide an extent.
  - Introduced buffer-based allocation for containers (Vector, SOA3D, AOS3D) 
  and several data structures (Quadrilaterals, Planes), enabling allocation-free 
  polycone/polyhedron creation on GPU.
  - UnplacedVolume::Normal is now a usable public interface.

Navigation & BVH
- Locate/Relocate fixes
  - Fixed long-standing bugs in LocatePointIn for both BVHNavigator and 
  LooperNavigator where points were passed in the parent frame while BVH expected 
  local coordinates.
  - Updated RelocatePoint to match the corrected LocatePointIn behavior.
  - Introduced LocatePointInNavState, taking a point in the local coordinate system 
  of an existing navigation state and returning the deepest containing state.
  - RelocateToNextVolume now passes correctly transformed points to 
  LocatePointIn and uses LocatePointInNavState for clarity and correctness.

- Generic navigation robustness
  - Improved handling of negative distances to daughters: points with negative 
  distance due to a push are now treated as inside, avoiding extra relocation 
  and potential field-propagation errors.
  - When exiting, navigation now finds the deepest mother containing the point 
  instead of relying solely on UnplacedContains, preventing incorrect relocation 
  from surface points.
  - Older navigators synchronized with these changes; removed obsolete debug/dead code.

- NavigationTable and large geometries
  - Removed harmful truncation of the number of daughters to short in 
  NavigationTable, which could crash very large detectors (>160k daughters, 
  e.g. ATLAS ITKPixel).
  - Added a protective throw when limits are exceeded.
  - Navigation fixes and cleanups tied to large-geometry support.

- BVH improvements and unification
  - Unified BVH implementations: removed the diverged BVHSurf, merged useful 
  functionality into the main BVH, and removed AABBSurf and BVHNavigatorV.
  - Increased tolerance in ComputeIntersectionInvDir from 10 to 500 ULP to avoid 
  false hits inside approached AABBs.
  - Restored solids’ ComputeSafety implementation and made its internal precision 
  use Real_t, enabling precision-templated BVH variants.
  - Relaxed and simplified internal BVH checks; fixed uninitialized BVH 
  variables and unused surface-BVH branches.
  - Added ABBoxManager_t alias for forward compatibility with a templated 
  ABBoxManager (precision-dependent surface BVH).

- NavState utilities
  - Extended NavStateIndex / NavStateTuple:
    - World() getter for the world placed volume.
    - Constructor from a container of state indices.
    - GetLastExitedState() and SetLastExited() for tracking last exited volumes.
    - IsValid(state) to validate indices/tuples.
    - Print(print_names) option to print volume names along a navigation path.
  - NavStatePath::GetLastExited now guards against out-of-range indices.

CUDA, GPU & RDC
- Integrated CudaRdcUtils (v12 and then updated to v13) and synchronized 
with upstream CudaRdcUtils.cmake.
- Introduced a VecGeomCUDA project and forward-ported RDC settings and 
libraries.
- Improved CUDA RDC handling in CudaRdcUtils:
  - Avoid propagation of -Wl,--no-undefined when building CUDA RDC 
  intermediate libraries.
  - Explicitly allow undefined symbols via platform-specific options 
  (e.g. allow-shlib-undefined, -z undefs).
  - Added missing cudart linkage for non-CUDA libraries depending on CUDA ones.
- Propagated CUDA include paths to the host compiler to simplify builds.
- Enabled single-precision support in the solid BVH and allowed CudaManager 
to accept a null “world” pointer on device.
- Switched device code to use VecCore math routines instead of <cmath> 
for consistency and performance.
- Fixed GPU construction issues for cones/polycones with sections where 
rmin = rmax by constructing with the original rmax and aligning CPU/GPU behavior.
- Removed broken CUDA RNG implementation.

Logging, Diagnostics & Device API
- Introduced a unified logger, contributed from Celeritas, with documentation 
and adoption across core modules and VGDML.
- Reduced spurious “calling host from host/device” messages and refined 
log levels for cleaner diagnostics.
- Replaced cout/printf with cerr and guarded CUDA output appropriately.
- Implemented a device-portable API layer:
  - BVH Manager device-pointer access APIs and VECGEOM_BVHMANAGER_DEVICE 
  macro for downstream consumers.
  - A set of device-portable assertion and API call macros for CPU/GPU builds.

Portability Macros & GPU Utilities
- Added new GPU portability and exception-handling macros, including:
  - VECGEOM_DEVICE_API_CALL for CUDA/HIP-style API calls and error checking 
  (currently CUDA in use).
  - VECGEOM_DEVICE_API_SYMBOL for portable symbols.
  - VECGEOM_ASSERT and VECGEOM_VALIDATE for always-on runtime checks (throwing 
  std::runtime_error on failure).

Configuration, Build System & Tooling
- Introduced and refined CMake-based versioning:
  - Added full VecGeom version strings to exported configuration and proper 
  version provenance for non-point releases.
  - Added cmake-git-version support with improved hints for missing metadata; 
  updated version matching for vX.Y.Z-dev.W.
  - Fixed version-finding scripts for archived versions and refined automatic 
  versioning based on Git branches.
- Configuration and CI:
  - Enforced out-of-source builds to avoid confusing warnings and stray 
  build products.
  - Updated tested CMake and compiler versions (including GCC 12–15, 
  Clang 16/19) and migrated CI to newer CUDA and platforms (e.g. cuda12 on alma9).
  - Fixed Geant4 11.2 linking issues (split G4Persistency libraries) and 
  restored Geant4-based CI builds.
  - Updated Docker images and expanded tested configurations.
  - Added optional GDML validation controlled by command-line arguments.
- CMake options & tooling:
  - Refactored CMake options and definitions; fixed version-string logic.
  - Ensured final VecGeom library is retained with --as-needed linker flags.
  - Added CMake fallback for cuda_rdc_target_sources when using installed VecGeom.
  - Made Doxygen generation optional via VECGEOM_DOXYGEN (default OFF).
  - Improved intelligent CMake defaults and removed surprising ccache behavior.
  - Updated .gitignore to allow storing CMake preset files in the source directory.

Compiler Compatibility & Warnings
- Fixed issues with:
  - Clang 16/19, including missing template arguments, device annotations, 
  visibility attributes, and nonexistent device functions.
  - GCC 12.3, GCC 14, and GCC 15 (warnings and diagnostics).
  - macOS-specific warnings and uninitialized constructors.
  - GNU/Hurd build failures.
- Enabled proper compiler flag setup via CMAKE_CXX_COMPILER_ID and corrected 
logic that previously referenced unset variables.
- Fixed -Wreorder warnings (e.g. initialization order of BVH::fDepth).
- Added VECGEOM_PRAGMA_UNROLL(N) for portable loop unrolling, used among 
others in GenericTrapezoid and to remove unroll-related warnings.
- Fixed -Wfloat-conversion by eliminating implicit double–float casts.
- Addressed stack and object-size warnings (e.g. for ReducedPolycone) and 
adjusted stacks for CMS GPU CI tests.

Base Containers & Memory Safety
- Vector:
  - Fixed destructor behavior for composition cases where the same object 
  could be destroyed multiple times.
  - Cleaned up possible double-destruction and added extensive tests 
  (TestVector, including nested Vector<Vector<T>>).
- Memory safety
  - Prevented reads of destructed memory (closing VECGEOM-624) by strengthening 
  object lifetimes.
  - Fixed several uninitialized constructors and undefined behavior in 
  core components.

API & Namespace Changes
- Removed legacy vector APIs:
  - Eliminated vector interfaces from VNavigator, VPlacedVolume, VUnplacedVolume, 
  VSafetyEstimator, and related helpers.
  - Removed vector-based tests and benchmarking code, including Geant-V 
  specific functionality from LogicalVolume.
- Simplified implementation helpers and removed Geant-V specific or unused 
APIs:
  - Removed GetTransCode, GetRotCode, Rectangles, Lorentz headers,
  SpecializedTransformation3D, unused MessageLogger, custom Map, and other 
  dead code.
  - Removed extended DistanceToOut function variants that were not used 
  by any shape.
  - Moved PrintType to placed volumes and removed unused printing helpers.
- Namespace hygiene:
  - Moved Precision, NavIndex_t, and uint_t out of the global namespace 
  into vecgeom/vgbrep.
  - Added VECGEOM_PRECISION_NAMESPACE macro to preserve source compatibility 
  with dev and v1.x releases.
  - Updated CUDA paths and dependent code accordingly.
- Rotation and transformation:
  - Removed template-based rotations and obsolete typedefs (RotationCode,
  TranslationCode); fHasRotation is now handled directly.
  - Transformation3D is now compatible with G4AffineTransformation in Geant4, 
  making VGDML-produced transformations numerically compatible with 
  Geant4’s G4GDMLRead.

Persistency & VGDML
- Fixed rotation handling for Boolean components in VGDML:
  - Corrected for row-major vs column-major rotation layouts between 
  VecGeom and Geant4 using transposed elements.
- Fixed conversion of Boolean subtractions to Geant4, ensuring proper 
rotation for the subtracted node.
- Improved memory setup (stack/heap) to pass GeometryTest for CMS geometries.
- Switched VGDML volume registration to map lookups instead of linear 
search, significantly speeding up conversion.
- Added a generic benchmark to measure performance of a volume loaded 
from a GDML file.
- Adopted a singleton pattern for XercesC initialization with a proper 
destructor for clean shutdown.

Miscellaneous
- Matched polycone phi-angle readjustment to Geant4 (and UPolycone wrapper), 
resolving inconsistencies (e.g. CMS D110 Run4 geometry).
- Reduced verbosity in FillUncontainedPoints() and moved FlatVoxelManager 
messages to diagnostic logger levels.
- Added ABBoxManager_t alias and other forward-compatibility hooks for 
future precision-templated BVH.
- Ensured Unit() does not perturb the magnitude of generated unit vectors; 
user must avoid calling Unit() on null vectors.
- Numerous small documentation updates, annotation fixes, and code cleanups 
across the tree.

Version 2.0.0-rc9      18th November 2025
=========================================
This release candidate fixes a bug RelocateToNextVolume passing points in
the coordinates of vol to LocatePointIn, instead of the parent of vol.
To avoid further confusion, the cleaner and simpler function 
LocatePointInNavState is used.

Version 2.0.0-rc8      13th November 2025
==================================================
This release candidate includes important fixes and improvements across
navigation, BVH accuracy and consistency, CUDA RDC handling, compiler
compatibility, and general code robustness. It also removes deprecated or
broken components and improves documentation.

- Navigation and geometry fixes
  - Fixed a long-standing bug in `LocatePointIn` in both `BVHNavigator` and
    `LooperNavigator`. The function received points in the *parent* reference
    frame of a volume, but the BVH expected points in the *local* frame.
    The missing transformation caused incorrect navigation for all but the
    world volume. The behavior is now corrected and documented.
  - Updated `RelocatePoint` to match the corrected `LocatePointIn` behavior.
  - Added a new `LocatePointInNavState` method, which takes a point expressed in
    the local coordinate system of an existing navigation state and finds the
    deepest state containing that point.
  - Matched polycone phi-angle readjustment to Geant4 (and UPolycone wrapper),
    resolving inconsistencies observed in the CMS D110 Run4 geometry.

- BVH improvements and cleanup
  - Unification of BVH and BVHSurf: removed the diverged `BVHSurf` and merged
    all functionality into the main BVH implementation. Removed also
    `AABBSurf` and `BVHNavigatorV`, transferring remaining useful functions
    into BVH.
  - Increased error tolerance for `ComputeIntersectionInvDir` from 10 to
    500 ULP to avoid false hits inside approached AABBs.
  - Restored solids’ version of `ComputeSafety`.
  - Made internal `ComputeSafety` precision use `Real_t`, avoiding
    unnecessary casts and enabling precision-templated BVH variants.
  - Relaxed and simplified several internal checks.

- CUDA and build-system updates
  - Removed broken CUDA RNG implementation.
  - Major updates to CUDA RDC handling in `CudaRdcUtils`:
      - Avoid propagation of `-Wl,--no-undefined` when building CUDA RDC
        intermediate libraries, which by design contain undefined symbols.
      - Explicitly allow undefined symbols via platform-appropriate options
        (`allow-shlib-undefined`, `-z undefs`) where available.
      - Added missing `cudart` linkage for non-CUDA libraries depending on
        CUDA libraries.
      - Updated CudaRdcUtils to version 13.
      - Removed dead code, debug remnants, and global linker flag restoration.
  - Fixed build errors with Clang 19 and CUDA 12.9:
      - Corrected device annotations and visibility attributes.
      - Fixed use of nonexistent device functions.
      - Added a warning when host compiler and CUDA compiler differ.
      - Fixed an issue where VecCore’s `check_language(CUDA)` erased
        `CMAKE_CUDA_HOST_COMPILER`. A wrapper for `find_package(VecCore)`
        now prints VecCore version and components at configure time.
      - Updated `.gitignore` to allow storing CMake preset files in the
        source directory.

- Compiler warnings and portability
  - Fixed two macOS warnings: an uninitialized BVH constructor variable and
    removal of an unused branch intended for surface BVH construction.
  - Enabled compiler flags properly: previous CMake logic referenced variables
    that were not set. Compiler flags are now selected using
    `CMAKE_CXX_COMPILER_ID`. Also fixed new warnings in `testRaytracing.cpp`.
  - Fixed `-Wreorder` warning by correcting the initialization order of
    `BVH::fDepth`.
  - Added portable pragma unrolling via `VECGEOM_PRAGMA_UNROLL(N)`, enabling
    consistent loop unrolling and removing warnings in the generic trapezoid.
  - Fixed warnings for gcc15.

- API and type namespace updates
  - Moved `Precision`, `NavIndex_t`, and `uint_t` out of the global namespace
    into `vecgeom`/`vgbrep`. This change improves namespace hygiene and
    consistency.
  - Added `VECGEOM_PRECISION_NAMESPACE` macro to preserve compatibility with
    previous dev and v1.x releases.
  - Updated CUDA paths and dependent code accordingly.

- Miscellaneous
  - Fixed minor annotation issues and improved documentation in several areas.

Version 2.0.0-rc7        25th September 2025
============================================
Overview
- Major rewrite and full stabilization of GenericTrapezoid (GenTrap),
fixing all known corner cases and improving both CPU/GPU performance. 
- Safer navigation in very large geometries; fix for harmful truncation 
in NavigationTable that could crash with >160k daughters.
- Better build tooling: CUDA RDC fallback for installed VecGeom,
togglable Doxygen generation, improved cmake-git-version script,
restored Geant4-based CI build.
- Numerous portability, warning, and correctness fixes.

Detailed changes
- Solids / Geometry
  - GenericTrapezoid scalar refactor with documented algorithms;
  Lipschitz-based conservative safety; inside/normal improvements; 
  caching and CUDA-side optimizations; additional edge-case tests 
  (EMEC bad aspect-ratio) and many micro-optimizations.
  - Fixed copy/paste error in `Utils3D::Polygon::Extent` mixing X/Y 
  in a comparison.

- Navigation
  - Remove harmful truncation in `NavigationTable` (number of daughters 
  truncated to `short`), which could crash large detectors (e.g., ATLAS ITKPixel).
  Added a protective throw.
  - Navigation fixes and cleanup tied to the GenericTrapezoid work.

- Build / CMake / Tooling
  - Add CMake fallback for `cuda_rdc_target_sources` for installed 
  VecGeom (improves CUDA RDC usability).
  - Make Doxygen generation optional via `VECGEOM_DOXYGEN` (default OFF).
  - Improve `cmake-git-version` script (VECGEOM-631): allow user-provided
  describe, better hints if metadata missing.

- CI / Testing
  - Fix Geant4-based builds and add a CI target using Geant4.
  - Add EMEC bad aspect-ratio trapezoid test; enable extra debug lines in
  shape tests.

- Portability / Warnings
  - Fix build error on GNU/Hurd.
  - Silence `<argument 1 value exceeds maximum object size>` warnings in
  `ReducedPolycone`.

Version 2.0.0-rc6        3rd July 2025
======================================
Base:
- Cleaned possible double destruction of Vector elements.
TestVector was extended to thoroughly test features, including 
Vector<Vector<T>>

Shapes:
- Fix construction on GPU of cones/polycones with sections with rmin = rmax
Avoid numerical differences between CPU and GPU by constructing with
original rmax

Version 2.0.0-rc5        25th June 2025
======================================
Base:
- The Vector destructor has been fixed for the composition case, where
  the same object may be deleted multiple times

Shapes:
- RZ polycones fixed. Fix for ReducedPolycone utility for segment 
  checks. Additional cleanup was done concerning passing vectors by
  value. A test was added for the polycone RZ constructor, which was
  failing in CMS.
- Fixed normal computation for scaled shapes and trapezoids
- Cleaning of the torus: some data structures were both in the
  UnplacedTorus2 class and in the torus struct.Initialization of the
  bounding tube was fixed.
- Exposed VUnplacedVolume::Extent as pure virtual interface

Navigation:
- Global navigation cleanups, tweaks and fixes:
  - Negative distance to daughters can be due to a push, and was 
  previously ignoring the respective daughter - now the point is
  considered inside, preventing an extra relocation step and even
  errors in case of field propagation.
  - If the distance to exiting is shorter, navigation needs to find
  the deepest mother containing the point. Before, UnplacedContains
  was used, so if the point was on surface it was considered inside,
  then the relocator wrongly located to that parent, while the track
  was exiting.
  - Synchronized older navigators with these changes and removed
  some dead code for debugging.
- Additions to NavStateIndex/Tuple:
  - World() - getter for the world placed volume
  - Constructor taking a container with state indices (tuple
  case)
  - GetLastExitedState() returning the last exited state as
  navigation index, or tuple
  - SetLastExited(state) where state is NavIndex_t or NavTuple_t
  - IsValid(state) adding a check for validity of the provided
  NavIndex/Tuple
  - Print takes an argument print_names printing names in the path
  on the host (default false)

Miscellaneous:
- Improvement of ArgParser: support for arbitrary scalar and vector
types via OPTION_SCALAR and OPTION_VECTOR.
- Added option to read navigation tuples and last exited state in
taytracing
- Fix configuring against latest VecGeom with CUDA disabled

Version 2.0.0-rc4        27th May 2025
======================================
Surface model:
- The 'frames' surface model (framework for accelerated modeling of
  embedded surfaces) was introduced. It provides a GPU-friendly
  implementation of Geant-like hierarchical 3D solid modeling based
  on bounded half-spaces. The model can be constructed transparently
  on the host based on an in-memory VecGeom solid model and deployed
  to the GPU via simple API.
- 'frames' provides header-only stateless navigation API on the GPU,
  similar to the interfaces used for navigating within the VecGeom
  solid model.
- The model supports a set of primitive half-space types, such as planar,
  cylindrical or conical, and a set of frame types to create bounded
  regions   on the half-space surfaces, such as window or ring. Introduces
  FramedSurface objects that store the intersection of solids with
  surfaces, enabling efficient navigation and reducing computational
  complexity.
- GPU-Friendly Design: Eliminates recursive calls and virtual functions,
  reducing register and stack usage, and enhancing occupancy on GPUs.
  This design addresses divergence issues inherent in the solid model.
- Mixed-Precision Support: Supports both double and mixed-precision
  computations, allowing for flexibility in balancing accuracy and
  performance based on simulation requirements.
- Utilizes BVH acceleration structures to expedite collision detection
  and ray tracing, achieving significant speedups.
- Demonstrates controlled memory footprints and conversion times across
  various detector geometries, ensuring scalability for complex
  simulations.
- Replaced the GPU solids hierarchy with an indexed LogicId/PlacedId
  schema for better performance and memory usage (surface model w/o solids)
- Ongoing Development: While already merged into the master branch, the
  surface model continues to evolve, with efforts focused on expanding
  support for additional solid types and further optimizing performance.

Portability macros
- New GPU portability macros added, used for setting up GPU and exception
  handling.
- VECGEOM_DEVICE_API_CALL portability macro (CUDA/HIP, only CUDA in use 
  for now) for calling and checking API functions.
- VECGEOM_DEVICE_API_SYMBOL to have portable symbols
- VECGEOM_ASSERT 
- VECGEOM_VALIDATE for always-on runtime error checking and throwing
  std::runtime_error if it fails

Shapes
- Contains: improved Contains() functions for all shapes to correctly
  handle points on surfaces and eliminate boundary artifacts.
- Generic Polycone: fixed construction logic to ensure valid geometry in
  all configurations.
- Polygon & Box: added a small tolerance in edge and face tests to prevent
  crashes during geometry relocation.
- Sphere: corrected the DistanceToIn() implementation for robust ray
  entry calculations.
- Polyhedron: introduced a well‐behaved Z‐plane index finder and repaired
  several issues in PolyhedronStruct.
- Tube: fixed a typo in the Y‐component of the normal vector for
  phi‐plane intersections.
- Boolean Solids: properly compensate internal pushes in subtraction
  operations to restore correct shape boundaries.
- Buffer-based allocation for all containers (Vector, SOA3D, AOS3D) and
  for several data structures (Quadrilaterals, Planes). Allocation-free
  creation of polycones and polyhedra on GPU based on this feature.
- UnplacedVolume::Normal became a usable interface

Navigation
- Enhanced global navigation robustness, including stability fixes for
  grazing rays for most solids. Added ShapeTester test for grazing rays.
- Fixed NavStatePath::GetLastExited to guard against out-of-range indices.
- Resolved invalid reference checks in TestBVHNavigator to prevent
  intermittent crashes.

Configuration
- Refactored CMake options and definitions; fixed the version‐string logic
  and added automatic versioning based on the current Git branch.
- Ensured the final VecGeom library is retained when using “as‐needed”
  linker flags.
- Updated CI Docker image location and expanded tested configurations for
  consistency across platforms.
- Corrected build failures under CUDA 11.5 with VecCore 0.8.2; addressed
  -Wmissing-template-arg-list-after-template-kw in Clang 19, linker/Clang 16
  warnings, and various GCC 14 diagnostics.

CUDA & GPU
- Integrated CudaRdcUtils v12: synchronized with upstream
  CudaRdcUtils.cmake, migrated VecGeom into a VecGeomCUDA project, and
  forward-ported RDC settings and libraries.
- Propagated CUDA include paths to the host compiler for seamless builds.
- Enabled single-precision support in the solid BVH; updated CudaManager
  to accept a null “world” pointer on the device.
- Switched device code to use VecCore math routines instead of std for
  consistency and performance

Logging & Diagnostics
- Adopted the unified logger for important messages in core modules and
  VGDML; updated documentation accordingly.
- Reduced spurious “calling host from host/device” warnings and refined
  middleware log levels for cleaner diagnostics.

Memory Safety
- Prevented reads of destructed memory (closed VECGEOM-624) by strengthening
  object lifetimes.
- Fixed several uninitialized constructors to eliminate undefined behavior.

Device API
- BVH Manager: added device‐pointer access APIs and introduced the
  VECGEOM_BVHMANAGER_DEVICE feature macro for downstream consumers.
- Defined a set of device‐portable API calls and assertion macros to ensure
  consistent behavior across CPU and GPU builds.

Persistency & VGDML
- VGDML conversion now uses a map lookup instead of linear search for
  registered volumes, greatly improving conversion performance.

Miscellaneous
- Removed the deprecated implementation namespace from GeoManager to
streamline the public API.
- Adopted a singleton pattern for XercesC initialization, including a proper
  destructor for clean shutdown.
- Fixed compilation warnings on macOS and resolved errors under GCC 15.

Version 2.0.0-rc3         3rd Oct 2024
======================================
Shapes:
- Polycone: added new definition of SafetyToOut() that uses the line segment
  that forms the polycone boundary for calculation of the distance; fixes
  fixes all known issues related to SafetToOut() function of Polycone.
- Torus: fix for phi cut in corner cases;  rescale in calculation to torus
  radius.

Volumes:
- Fix in VolumeUtilities.h to reduce verbosity of FillUncontainedPoints();
  using logger in this method. In FlatVoxelManager, changed output to logger
  from 'info' to 'diagnostic'.

Configuration:
- Let CMake handle CMP0135 policy warnings from the use of ExternalProject
  for builtin VecCore (feature compatible only with CMake 3.24 and newer).
- Updates in CI to move to cuda12 on alma9.
- Fixes for CI settings and updated package versions.
- Increased stack for cms gpu CI test.
- Added intelligent cmake defaults and removed "surprising" ccache usage.
- Fixed linking errors to Geant4 11.2 targets, for splitted G4Persistency
  library.
- Fixed compilation warnings on gcc-12.3 compiler.
- Fix unused variable warning. Fix TestBVHNavigator compilation warning.
- Make the GDML validation optional, according to command line argument.
- Avoid CMake export of XercesC_INCLUDE_DIR and similar, to avoid errors
  in finding XercesC by external packages.
- Removed a c++11 hack; due to the lack of fold expressions in c++11,
  a c++11 fallback had to be used. This is now obsolete.

Miscellaneous:
- Removed templation from rotations. Removed obsolete typedefs of
  RotationCode and TranslationCode, only needed to set 'fHasRotation'
  which is now done directly.
- Added ABBoxManager_t alias for forward compatibility with a templated
  version; in the 'surface_model' branch, the singleton class ABBoxManager
  becomes templated on a precision type to support single precision in the
  surface BVH independently on the compile time 'vecgeom::Precision'.
  ABBoxManager_t is an alias of the actual ABBoxManager version, to be used
  in dependent libraries for forward compatibility until the 'surface_model'
  branch is merged.
- Fixed implicit double to float casts, to get rid of warnings when
  compiling with '-Wfloat-conversion'.

Version 2.0.0-rc2         6th Jun 2023
======================================
Overview:
- Fix in SimpleExtruded shape implementation of DistanceToOut().
- Fix for Extent of Boolean solids.
- Fix in VGDML module.
- Added logger classes with related documentation, contributed from Celeritas.
- Fixes in configuration setup.
- Added cmake git version support.

Configuration:
- Add full vecgeom version string to exported configuration; this gives
  provenance to installed versions of VecGeom that *aren't* point releases.
- Export CMake cache variables with VecGeomConfig.
- Changed static assert to runtime test failure.
- Jenkins: explicitly target the PLATFORM instead of relying on links.
- Added cmake git version support.
- Modified version matching regex to match vX.Y.Z-dev.W.
- Enforce an out-of-source build; requested to avoid odd and/or confusing
  cmake and build warnings. Also assists in keeping source dir clean from
  build products that might get accidently commited.
- Bump CMake max version tested to current version.
- Bump GCC to 12.2 in Gitlab-CI.
- Fixed version finding script for archived versions.

Shapes:
- Fix for DistanceToOut() in simple extruded implementation.
- Fix for extent of Boolean solids; this fixed a major bug introduced by the
  commit 7894c5ee that changed the semantic of VPlacedVolume::Extent to compute
  the extent of the displaced volume rather than delegate to the unplaced
  extent calculation. The implementations of UnplacedBooleanVolume<op>::Extent
  were converting the extents of the Boolean components to the
  UnplacedBooleanVolume frame, so after the change this conversion was done
  twice.
- Use Geant4 convention for Polyhedron, interpreting negative/zero angle dphi
  as twopi.
- Avoid getting unit vector out as a zero-length one in sphere.

Navigation:
- Introduce BVH and Loop navigators from AdePT. BVH code refactoring.
- Removed implementation namespace and fixed GetPlacedVolume dispatching in
  BVHNavigator.
- Less copies for getting cached transformations from the navigation index
  table.
- Fix for TestVGDMLNavigation and improvements for debugging.

Persistency:
- Fix for the rotation used in VGDML for Boolean components.
  The rotation matrices have different layouts in VecGeom compared to Geant4,
  row versus column major, so the constructor based on rotation elements need
  to use transposed elements.
- Fix for conversion of subtractions to Geant4. The rotation of the subtracted
  node was not created and not used when converting to the corresponding Geant4
  solid.
- Setup stack or heap values as needed for some common CMS geometries to pass
  GeometryTest.
- Added generic benchmark of a volume from a GDML file.

General:
- Transformation3D is now compatible with G4AffineTransformation in Geant4.
  Transformation produced by VGDML are now numerically compatible with the
  oned produced by G4GDMLRead.
- Generated unit vector does not add a small number to the magnitude.
  This is needed for numerical compatibility with Geant4. The user is
  responsible for not calling Unit for null vectors.
- Added logger, contributed from Celeritas.
- Use cerr instead of cout and printf; added missing cerr CUDA guards.

Version 2.0.0-rc1         15th Jun 2023
=======================================
Overview:
- Removed vector APIs.
- Simplified ImplHelpers.
- Removed transformation specialization.

Configuration:
- Removed all obsolete CI/general scripts. Rationalised gitlab CI spec.
- Use official WebEOS domain for geant4-data site.

Code cleanup:
- Removed vector APIs:
  * Removed Benchmarker vector tests.
  * Removed vector interfaces from VSafetyEstimator and derived.
  * Removed vector interfaces from VNavigator and derived.
  * Removed vector interfaces from VPlacedVolume, VUnplacedVolume
    and implementation helpers.
  * Removed vector interfaces from navigation specializer, testers,
    benchmarker, and tests.
- Simplified ImplHelpers.
- Removed transformation specialization (not yet linking on device):
  * Enabling automatically VECGEOM_NO_SPECIALIZATION in CUDA mode since
    specialization + CUDA never worked together.
- Removed GetTransCode and GetRotCode.
- Removed APIs for Geant-V from LogicalVolume.
- Removed unused class Rectangles.
- Removed unused Lorentz headers and SpecializedTransformation3D.
- Removed unused MessageLogger.
- Deleted now unused Map implementation.
- Extra simplifications:
  * Polycone benchmark can test now specializations.
  * Move PrintType to Placed volumes.
  * Clean up unused code.
  * No shape implements the extended DistanceToOut function.
  * Removed PrintImplementationType() and PrintUnplacedType().
  * Removed unused code in CudaManager.cu and unused SOA.h.
  * Removed dead test.

Version 1.2.3            15th Jun 2023
======================================
Overview:
- In PlaneShell, removed broken optimization for Trapezoid.
- Fixes in Polyhedra to provides correct navigation for dphi larger than pi.
- Fixed errors in geometry synchronization for GPU.

Configuration:
- Fix for name clash when including X11/X.h.

Navigation:
- ApproachSolid must return zero if called from inside the bounding box.

Shapes:
- PlaneShell: removed broken optimization for Trapezoid:
  * PlaneShell's SafetyToIn and SafetyToOut contained what looks like an
    optimization for the case N == 4. However, the condition / branches
    were swapped so that the code would use the general loop for N == 4
    and assume exactly four planes for N != 4. This wasn't a problem in
    practice because VecGeom only ever uses PlaneShell with N == 4, for
    the Trapezoid.
  * Performance measurements show that the re-ordering of Max operations
    yields a very minor improvement of at most 1.5% - 2%. As it wasn't
    active up to now, just remove the code path. As such, no performance
    difference expected nor measured.
- Polyhedra now provides correct navigation for dphi larger than pi.
  * Contains() takes into account the fact that the phi planes responses
    must be OR-ed.
  * Inside() correctly finds the inside surface and outside regions and
    uses consistently kTolerance instead of a mix with kHalfTolerance.
  * DistanceToIn/Out() correctly deals with early returns that were
    inappropriate in the larger than pi cases.
- Planes and quadrilaterals support the non-convex case to support phi
  planes at angle larger than pi:    
  * The logic for Contains and Inside must change from AND to OR if the
    planes are delimiting a non-convex body.
- Improved and simplified Print() format for several shapes:
  * Implemented SExTru Print() and warn for unexpected SExtru parameters
    from GMDL.

GPU:
- Fix errors in geometry synchronization for GPU:
  * Only used 'placed' volumes in BVH.
  * Copy missing components to GPU.
  * Use ID comparison rather than pointer comparison to determine size.
  * Moved ReflFactory from vgdml to vecgeom module.
  * Fixed missing copy of constituent volumes to GPU.
  * Eliminated redundant 'find'.
  * Use cout instead of printf().
  * Fix out-of-range errors in volume lookups.

Benchmarks & Tests:
- Added six polyhedron topologies in both the benchmarher and shape tester:
  * 0-NoInnerRadii, 1-WithInnerRadii, 2-WithPhiSectionConvex,
    3-WithPhiSectionNonConvex, 4-ManySegments and 5-SameZsection.
- Adjusted TestPolyhedra to deal with changed surface tolerance from
  kHalfTolerance to kTolerance.

Version 1.2.2            10th Mar 2023
======================================
Overview:
- Rationalised use of VECGEOM_{GEANT4,ROOT} in library code.
  Minimised use of ROOT/Geant4 in main VecGeom library.
- Improvements and extensions to the VGDML parser.
- BVHNavigator now allows relocation using VNavigator interface.

Configuration:
- Rationalised use of VECGEOM_{GEANT4,ROOT} in library code.
- Minimised use of ROOT/Geant4 in main VecGeom library:
  * Create VecGeomTest library for all shared testing code.
  * VGM is only needed by G4GeoManager as private dependency.
  * Make GDML tests a subdirectory of main tests.
  * Cleaner organization, find all tests under one tree.
  * Isolate ConvertTo{Geant4,Root} functions as far as possible.
  * Moved G4GeoManager out of VecGeom interface, as the class is only used
    for global benchmarks and should not be part of the main VecGeom interface.
  * Removed unused ResultComparator includes.
  * Made Transformation3D::ConvertToTGeoMatrix() a static method and updated
    client code to use it.
  * Fully remove ROOT dependence in NavState{Index,Path}; migrated remaining
  * ROOT conversion function to free function only used in testing code.
  * Removed NavState{Index,Path}::GetCurrentNode().
  * NavState{Index,Path} ROOT conversion/access can be static
    Removed copy assignment from TGeoBranchArray as this is not used.
    Made conversion/access ROOT functions static, as they can be
    implemented using public methods.
    Updated tests to use static member functions.
  * Remove ROOT dependence of FlatVoxelHashMap.
  * Minimize ROOT use in FlatVoxelManager.
    Removed private functions that are never called anywhere. Migrated
    remaining static functions to free functions in anonymous namespace
    to hide as much as possible.
  * Make all Volume use of VECGEOM_{ROOT,GEANT4} consistently.
  * Removed ROOT usage which is no longer relevant.
    Code never uses ROOT, so remove VECGEOM_ROOT.
  * Removed unused, obsolete header.
- Include config header to pick up VECGEOM_GEANT4; the symbol is no longer
  transmitted by compiler flag.
- No longer use VECGEOM_TEST_BENCHMARK in VecGeom library code.
- Use optional VMC from official upstream repository.

Navigation:
- BVHNavigator now allows relocation using VNavigator interface.
  Implemented VNavigator::Relocate() interface and related auxiliary helper
  methods; working on both host/device.

Persistency:
- Improvements to VGDML parser:
  * Make Trap theta,phi recalculation transient, moving from construction
    to accessor. Fixed sign conventions for Trap angles as in Geant4.
  * Fixed EllipticalCone shape parsing.
  * Fixed memory leak.
  * Fixed construction arguments for full compatibility with Geant4
    in conversion.
  * Use names from GDML files when creating the volumes.
  * Improved handling of no-rotation identity matrix.
    In addition to being much faster, this also fixes the previous
    no-rotation identity matrix, obtained by actual rotations of zero angles.
  * Fixed unit treatment for some shape parameters from GDML parsing.
  * Added meaningful parameter printouts for Tet and Boolean shapes.
- Added VGDML support for GenericPolycone shape.

Version 1.2.1            29th Nov 2022
======================================
Overview:
- Fixes in shapes: polycone, cone, tube.
- Extended vgdml support for the existing shapes.
- Configuration updates and reorganisation for tests and benchmarks.
- Improved CUDA support for memory allocation from shapes.
- Simplified daughter crossing check in navigation.

Configuration:
- Split service code into vecgeom/tests as appropriate. Moved benchmarking code
  to dedicated test-only library. Made utilities/ code a test-only library.
  Updated build scripts to account for path changes.
- Compile ShapeTester code into test-only library.
- Updated location for test files download.
- Ensure asserts are always enabled in all tests.
- Fixed compilation warnings on gcc-12 and on recent clang versions.

Navigation:
- In VNavigator, simplified the logic which uses the mother volume convexity to
  prioritize daughter checks. This approach is not always justified because
  it may increase the search range for daughter candidates. The current
  approach is conservative and uses the distance to mother first to limit
  the search distance for daughters, only if there are daughters. The check
  for daughters at this point spares extra checks in the derived navigators,
  which in the case of BVH was even generating a crash.

Persistency:
- Added vgdml support for UnplacedEllipticalTube, UnplacedEllipticalCone
  and UnplacedEllispoid shapes.
- Fixed reading of (simple) extruded solid.

Shapes:
- Updated Polycone/Cone implementation to only use kConeTolerance.
  Fixing cases of stuck tracks in ATLAS simulation.
- [VECGEOM-542]: In Tube::DistanceToOut(), added logic to detect point location
  in Phi sector of a tube. Now SafetyToOut() uses Wedge to calculate the
  SafetyToOut() for a point from the phi section.
  These modifications fix the mismatches reported by ShapeTester.
  Since now checking the existence of a point in Phi section in DistanceToOut(),
  a minor performance penalty is expected.
- [VECGEOM-602] Fix in Polycone::SafetyToIn() to remove unececessary
  calculations, when a point is outside the Z range.
- Rewritten PolyhedronStruct::Initialize() without use of dynamic memory.
  CUDA has only a very limited amount of scratch space for malloc / new.
  Having many polyhedra in a geometry setup may exhaust that space.

GPU:
- Expose function to change heap limit in CUDA.
  As of now, all "dynamically sized" shapes such as polyhedra, polycones,
  and extruded solids allocate memory in kernels. This is problematic as
  CUDA only has a very limited amount of scratch space for malloc / new.
  Until the "transfer code" for these types of shapes is fixed, add the
  function CudaDeviceSetHeapLimit() similar to CudaDeviceSetStackLimit()
  to let VecGeom clients work around this problem.
- Make BVHSafetyEstimator and BVHNavigator usable on device.
  Function annotation as host device as needed. Implement the Instance method
  for the GPU case.
- Overload Transformation3D::Set for aligned/byte-wise data; allowing
  user to choose whether a byte-wise load of the passed translation/rotation,
  or an element-wise. The latter resulting in better load instructions.
  Remove obsolete byte-wise Transformation3D::Set().
- Ensure that transformation data in nav table is stored aligned;
  fixed size calculation of navigation index table.

Miscellaneous:
- Added protection when using BVH navigation in leaf volumes.
- Added implementation of marching cubes algorithm.
- Fixed pixels container overlaps for trackML GDML file.
- Self-consistency fixes for header inclusions in translation units.

Version 1.2.0             4th May 2022
======================================
Overview:
- Configuration updates: updated configuration flags.
- Switch to C++17 by default.
- Some code optimisations for GPU.

Configuration:
- Bump minimum CMake version to 3.16.
- Switch to C++17 by default.
- Use new-style project arguments.
- Use same C++ standard for C++ and CUDA.
- Fixed CudaAssertError in release builds.
- Facilitate error tracing in CudaManager.
- Force cache CUDA-required option and check CUDA Compiler ID/Version
  compatibility.
- Install python symlink package on CI image.
- Mark ccache program option as advanced and group options by build/test.
- Drop linking libraries from custom ClangTidy check.
- Migrated settings flags:
  o SINGLE_PRECISION to VECGEOM_SINGLE_PRECISION
  o FAST_MATH to VECGEOM_FAST_MATH
  o USE_CACHED_TRANSFORMATIONS to VECGEOM_USE_CACHED_TRANSFORMATIONS
  o EMBREE to VECGEOM_EMBREE
  o GDML to VECGEOM_GDML
  o USE_NAVINDEX to VECGEOM_USE_NAVINDEX
  o USE_INDEXEDNAVSTATES to VECGEOM_USE_INDEXEDNAVSTATES
  o INPLACE_TRANSFORMATIONS to VECGEOM_INPLACE_TRANSFORMATIONS
  o DISTANCE_DEBUG to VECGEOM_DISTANCE_DEBUG
  o QUADRILATERAL_ACCELERATION to VECGEOM_ENABLE_QUADRILATERAL_ACCELERATION
  o PLANESHELL to VECGEOM_PLANESHELL
  o NO_SPECIALIZATION to VECGEOM_NO_SPECIALIZATION
  o BUILTIN_VECCORE to VECGEOM_BUILTIN_VECCORE
  o BACKEND to VECGEOM_BACKEND
  o ROOT, Geant4 options to VECGEOM_ namespace
  o VECGEOM_BENCHMARK to VECGEOM_TEST_BENCHMARK
  o VECGEOM_VTUNE to VECGEOM_TEST_VTUNE
  o BENCHMARK to VECGEOM_TEST_BENCHMARK
  o STATIC_ANALYSIS to VECGEOM_TEST_STATIC_ANALYSIS
  o VALIDATION to VECGEOM_TEST_VALIDATION
  o COVERAGE_TESTING to VECGEOM_TEST_COVERAGE
- Clarification of compiler flag setup and documentation.
- Updated static analysis configuration.

GPU:
- Moved TopMatrixImpl into header and made iterative; moved some BVH functions
  and globals to header, improving performance in AdePT.

Primitives:
- Cone, Polycone: consistently replaced kHalfConeTolerance with kConeTolerance.

Miscellaneous:
- Make NavStateIndex trivially destructible.
- EmbreManager: fix Wsign-compare.
- Updated MaskedAssignCheck to avoid catching trivial expressions.
- Use Ubuntu 22.04LTS (Jammy) for CI code checks.
- Removed obsolete and unused Nuclei code.

Version 1.1.20         30th March 2022
======================================
Overview:
- [VECGEOM-540] Fixed stuck tracks during propagation for CutTubs reported in
  CMS simulation. Increased tolerance for negative solutions of tube second
  order solver.
- Preventing false misses in AABB::Intersect().
- Speed up BVH construction.
  Improved BVH construction for concentric objects.

Persistency:
- Added support for physnode::scale tags with reflections.
  Added support for scale in Transformation3D.

GPU:
- Added bulk copy functions for UnplacedPolyhedron and UnplacedPolycone.
  Activate bulk copy of UnplacedVolumes in CudaManager, reducing the time
  to construct instances on device.

Miscellaneous:
- Refactored GeometryTest to be non-recursive, extended to probe unplaced safety.
- Fix for Embree support.
- Fixes in printouts.

Version 1.1.19         11th March 2022
======================================
Overview:
- Improvements and optimisations in BVH:
  o Initialise memory in BVH nodes. Added tree printing to BVH.
  o Added a new new splitting heuristic for BVH.
  o Added surface area heuristic for BVH construction.
    Make it the default for BVH construction.
  o Added SurfaceArea() to AABB. Use AABB::SurfaceArea for BVH construction.
  o Stabilised AABB::Contains().
- Modernisation of CMake usage.

Migration / behaviour change(s):
- Allow for various construction algorithms in BVH.
  Moved existing splitting algorithm into dedicated function.
  By implementing an alternative function, one can easily adapt and
  compare splitting algorithms.
- [VECGEOM-307] Removed obsolete interface SafetyToInMinimize.
- Updated README and INSTALL documents. Merged INSTALL into README.
  Modernized the README, adding detailed requirements and notes on how
  to use the VecGeom CUDA library.

Configuration:
- Small modernisation of CMake target usage in place of variables.
- Use genexes to simplify library link choice.
  CMake can resolve targets without genex.
- Explicitly use builtin VecCore in GitLab CI; set BUILTIN_VECCORE to ON
  explicitly in build jobs on GitLab.
  Explicitly use system VecCore in Jenkins builds; add BUILTIN_VECCORE=OFF
  to default build arguments to make this use case explicit.
- Do not fallback to builtin VecCore if external not found.
  Support switch between builtin/external in current build directory to
  ensure consistent builds.
- Only use VECGEOM_ prefix internally.
- Streamlined variables/targets/components in VecGeomConfig.
- Removed obsolete local cmake_parse_arguments function.
  Removed the local implementation, relying on CMake's native support.
- Simplified cmake config of gdml.
- Moved creation of benchmark executables to test/benchmark.
  TestNavIndex is now automatically disabled when GDML is not active.

- Protect C++ compile options from direct use by NVCC.
  Limit direct use of C++ compile options to C++ compilation consumers.
  Wrap C++ compile options in `-Xcompiler=` flags for CUDA consumers to
  forward these to the host compiler for both CUDA 10 and newer.
  This can be dropped once VecGeom requires CUDA 11 or newer as the
  minimum version.
- Simplified VecGeom specific CUDA options. Removed no longer required option
  for CUDA static/shared runtime; unified CUDA volume specialization under one
  option.
- Suppressed CUDA stack size warnings in device link.
- Break a dependency from libvecgeom to libvecgeomcuda.
- Removed CMake FindCUDA module.
- Renamed the CMake option CUDA to VECGEOM_CUDA.
- Unified VECGEOM_CUDA with VECGEOM_ENABLE_CUDA. Use VECGEOM_ENABLE_CUDA
  as single CMake boolean to request/indicate use of CUDA.
- Hand choice of cuda architecture over to CMake.
- Provide static and shared variants of libvecgeomcuda.
  Use native CMake CUDA support to build static and shared variants
  of the vecgeomcuda library.
  o The static variant is a "standard" CUDA library that consumers
    can link/device link to.
  o The shared variant includes a device link object so that non-CUDA
    consumers can link to it without requiring an otherwise unneeded
    device link.
  o CUDA consumers can link to the shared variant provided they
    device-link explicitly to the static vecgeomcuda.
  Do not link the VGDML library to either CUDA variant of vecgeomcuda,
  allowing clients to use the desired static/shared variant.   
  Updated tests to use only static variant of vecgeomcuda when available.
- Support CMAKE_CUDA_ARCHITECTURES for CMake<3.18.
  Added flags to `CMAKE_CUDA_FLAGS` using same pattern as CMake >= 3.18's
  native support, which is equivalent to the old `-arch=sm_<ARCH>` shorthand.
- Modernised test dependencies.
- Build TestNavigationStatePool for CPU/CUDA.
- Fixed missing vecgeomcuda symbols in vgdml.

- Fixed build with SINGLE_PRECISION.

Details:
- Use C++11 in-class initialisers for AOS, SOA and Array classes.
  Small modernisation of initialisers and constructors for storage classes.
- Suppressed allocation for zero-size AOS3D, SOA3D and Array.
- [VECGEOM-522] Removed redundant lines (no side effects) in ConeImplementation.
- [VECGEOM-512] Homogenized Placed<Shape>::PrintType() implementations.
- [VECGEOM-597] Fixed uniformity issue in SamplePointOnSurface() for Tube.
- Removed a useless Vector3D..
- Removed unused static counter.
- Removed __host__ __device__ specifiers from defaulted constructors.
- Fixed conversion factors for safety in scale transformations in case of
  reflections.
- Fixed AssemblyTest.

Version 1.1.18       5th November 2021
======================================
Overview: 
- Further implementation of functions needed for inclusion of VecGeom
  Navigation into Geant4.
- Moved included version of VecCore to 0.8.0
- Use precision-dependent push rather than fixed one.
- Fix for GeometryTest in CUDA + vector mode
- More improvements and fixes to multiple shapes for improved test results
  in single precision mode
- Fixes for single-precision mode on GPU.

Migration / configuration / behaviour change(s):
- Added arb8 to gdml reader.
- Export required flags for VecGeom as compile options.
- Disable usage of fast math for nvcc unless requested via FAST_MATH option.

Details:
- VNavigators (HybridNavigator2, NewSimpleNavigator) improved.
- VolumeUtilities::FillUncontainedPoints revised to cope with no uncontained
  volume.
- Improvements for different compilers: Intel, LLVM, Clang 13.0
- Type consistency fixes.
- More robust type casting for utility methods.
- Compilation fixes for single-precision vector mode.
- Use vecCore::math function rather than standard ones, for consistent
  host/device behaviour.
- Improved cone performance: avoid a few redundant IsOnConicalSurface(...)
  function calls.
- Add Normal() CPU implementation also to GPU.
- Added doxygen docs for ApproachNextDaughter.
- BVH: Declare stacks as unsigned int and reduced code duplication of
  LevelLocate.

Version 1.1.17      3rd September 2021
======================================

Overview:
- Improvements and fixes to multiple shapes to enable tests to pass in single
  precision mode.
- Fixes for BVH.
- Added accessors for navigation state index.

Migration - configuration or interface changes:
- Removed navigation index setter since the state should rather be recomputed
  for consistency.

Details:
- Fix for on surface and moving out condition for grazing rays on sphere.
- Fix for BVH::ApproachNextDaughter: point/dir conversion to checked daughter
  frame.
- Fixes for cut tube DistanceToIn. Precision fixes for the cut tubes.
- Fixed cone shape tester. Consistent Inside tolerance check for cone.
- Fixed approaching bbox and removed leftover debug checks.
- Fixed convention checking for points on edges.
- Fixed formula for MakePlus/MinuTolerantSquare.
- More precision fixes in the ShapeTester, improved use of ApproachSolid
- Tolerance-related fixes for Tet to pass the ShapeTester.

- Introduce standalone kFarAway in ShapeTester and further reduce errors.
- Fix types in ConventionChecker and ApproxEqual.

- Unify construction of Quadrilaterals.
- Consistently mark functions & destructors with VECCORE_ATT_HOST_DEVICE.

- Use VECGEOM_CONST for constants of kTolerance.
- Rename kernel function for ConstructManyOnGpu.
- UnplacedTrd now exposes interface 'normal' on GPU. Value is computed even
  if point not exactly on surface.
- Transformation3D::Set copies from memory only if has_trans or has_rot are set.
- Added new method to approach the next daughter to its bounding box.
- Fixes for navigation index table in single precision mode.
- Add GPU transfer testing for cms2018 geometry.
- (upstream/fix_bboxes_gpu) Fix bounding boxes on GPU by copying the values
  calculated on host.
- Fix per solid tolerances and change types from double to Precision.

- Add bounding box tests to ctest.
- Implement ApproachSolid utility and store BBox in UnplacedVolume.
- Fix Polyhedron::Contains() around phi tolerance.
- Remove unused ScalarSegmentContainsKernel.
- Activate bulk copying of PlacedVolume and Transformation3D to the GPU.
- Implement functions for bulk copying Transformation3D instances to GPU.
- Implement functions for bulk-copying of placed volumes to GPU.
- Improve const correctness in CudaManager.
- Move verbose printouts to cout instead of cerr in CudaManager.
- Add "Testing/" to .gitignore, which is created by ctest.

- Reduce HOST_DEVICE attribute for PlacedVolume::GetName().
- Add geometry test that compares CPU and GPU geometries.
- [CMake] Remove -G from RelWithDebInfo.
- [clang-format] Prevent clang-format from breaking doxygen comments.
- [git] Extend git ignore to "build*" instead of build.
- [NFC] Make detailed BVH description show up in doxygen.
- [NFC] Add docstring for VECGEOM_DEVICE_INST_PLACED_VOLUME.
- Fix failing test by creating new utility function.
- Fix failing tests in single precision.
- Change kTolerance to kHalfTolerance.
- [VECGEOM-578] Fix an FP comparison that was failing on the GPU.
- Fix a typo in debugging printout.
- [CMake] Fix doxygen search path.
- Fix TestOrb unit test when using single precision.
