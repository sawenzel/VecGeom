#include "LibraryGeneric.h"
#include "KernelTube.h"
#include "Tube.h"

bool Tube::Contains(Vector3D<double> const &point) const {
  // NYI
  return false;
}

double Tube::DistanceToIn(Vector3D<double> const &pos,
                          Vector3D<double> const &dir,
                          double const step_max) const {
  // NYI
  return 0;
}